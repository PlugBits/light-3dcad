# コントリビューションガイド

light-3dcad はブラウザだけで動く日本語ネイティブのパラメトリック3D CADです。バグ報告・機能提案・
Pull Requestを歓迎します。

## 開発環境のセットアップ

```bash
git clone https://github.com/PlugBits/light-3dcad.git
cd light-3dcad
npm install
npm run dev   # http://localhost:5173 で開発サーバーが起動します(base=/)
```

Node.jsのバージョンは `package.json` / CIの設定に準拠してください。特別なネイティブビルド環境は
不要です(Replicad/opencascade.js・PlaneGCSともにWASMをnpm経由でそのまま取得して使用します)。

## 変更を送る前に通すゲート

Pull Requestを送る前に、最低限以下をローカルで通してください(CIでも同じチェックが走ります)。

```bash
npx tsc --noEmit -p tsconfig.app.json   # アプリコードの型チェック
npx tsc --noEmit -p tsconfig.e2e.json   # E2Eコードの型チェック
npm test                                # Vitest(単体・統合テスト)
npm run e2e                             # Playwright E2E(時間がかかる場合はシナリオを絞って実行可)
npm run build                           # 型チェック(tsc -b)+ 本番ビルド
npm run size                            # メインバンドルサイズの回帰チェック
```

`src/`配下を変更した場合は特に、既存のフィーチャーツリー・スケッチ拘束・エクスポート等の挙動を
壊していないか、関連するVitest/E2Eのアサーションも合わせて更新してください。

## コミット規約

- コミットメッセージは**日本語**、現在形・命令形(例: 「〇〇を追加する」ではなく「〇〇を追加」)で
  簡潔に書いてください。
- **1コミットは小さく、意味のある単位で**分割してください。無関係な変更(フォーマットのみの変更、
  関係ないファイルの整理など)は別コミットに分けてください。
- 可能であれば、何を・なぜ変更したかが後から`git log`だけで追えるようにしてください。

## Pull Requestについて

小さな修正から大きな機能追加まで、Pull Requestは歓迎です。大きな変更(新しいフィーチャー種別の
追加、内部データ構造の変更など)を検討している場合は、実装前にIssueで方向性を相談していただけると
スムーズです。

## モデルギャラリー(コミュニティモデル共有)について

`models/` ディレクトリがコミュニティモデルの投稿場所です。ここに追加された`.l3dcad`モデルは、
ビルド時に静的なギャラリーページ(`dist/gallery/`、アプリのリボンの「ギャラリー」リンクから開けます)
として自動的に一覧化され、`?g=<slug>` のURLでアプリから直接開けるようになります。

### 投稿方法(おすすめ): アプリ内「ギャラリーに投稿」ボタン

Gitやフォークの知識が無くても、**GitHubアカウントだけ**あれば投稿できます。

1. アプリでモデルを作成し、リボンの「ギャラリーに投稿」ボタンをクリックする。
2. ダイアログにタイトル・作者名・説明・タグ(任意)を入力して「送信」を押す。GitHubのIssue作成
   画面が新しいタブで開き、入力内容とモデルデータが自動的に入力されています(モデルデータが長すぎる
   場合は自動入力できないことがありますが、その場合は共有リンクがクリップボードにコピーされるので、
   「モデルデータ」欄に貼り付けてください)。
3. Issueフォームの「ライセンス同意」にチェックを入れて送信する。
4. あとは自動化されたワークフロー(`.github/workflows/model-submission.yml`)が内容を検証し、
   問題が無ければ自動的にPull Requestを作成します(不正な内容の場合はissueにコメントで案内が
   付き、`invalid`ラベルが付きます。issueを編集して再送信すれば再検証されます)。
5. Pull Requestがメンテナによってマージされると、ギャラリーに掲載されます。

### 投稿方法(上級者向け): Pull Requestを直接作成する

Gitに慣れている場合は、`models/`ディレクトリへ直接Pull Requestを送ることもできます。

1. リポジトリをフォークし、`models/<slug>/` ディレクトリを作成する(`<slug>`は英数字とハイフンのみ、
   ディレクトリ名がそのままギャラリー・URLでの識別子になります)。
2. `models/<slug>/model.l3dcad` を追加する。アプリの「保存」ボタンでダウンロードした`.l3dcad`ファイルを
   そのまま置くのが簡単です(`npm run dev` でアプリを起動し、モデルを作って保存してください)。
3. `models/<slug>/meta.json` を追加する。以下の4フィールドすべてが必須です。

   ```json
   {
     "title": "モデル名(日本語可)",
     "author": "あなたの名前・ハンドル名",
     "description": "モデルの簡単な説明",
     "tags": ["タグ1", "タグ2"]
   }
   ```

4. `npm test` を実行し、`tests/project/models.test.ts` が投稿したモデル・meta.jsonを自動検証する
   ことを確認する(壊れたモデルやスキーマ不備はCIで落ちます)。
5. Pull Requestを作成する(`.github/PULL_REQUEST_TEMPLATE/model.md` のチェックリストを使うと簡単です)。

### ライセンス

`models/` への投稿(モデルデータ・メタ情報)は、このリポジトリの他のコードと同様に**MITライセンス**の
下で提供されるものとします。第三者の著作物を含めないでください。

### 制約

- モデルは実際にアプリ(`?g=<slug>`)で開けて評価が成功する必要があります(壊れたフィーチャーデータは
  バリデーションで弾かれます)。
- 部品配置(`partInstance`、簡易アセンブリ)を含むモデルも投稿可能です。
